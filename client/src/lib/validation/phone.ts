/**
 * Phone validation — re-exported from fieldSchemas (libphonenumber-js engine,
 * same as <PhoneInput />). Use `fieldSchemas(lang).phone` / `.phoneOptional`
 * for schemas, or isValidPhone() for a boolean.
 */
export { isValidPhone } from './fieldSchemas';
