node_modules
# dist - REMOVED: Must be included in Cloud Run deployments (built before deploy)
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# GCS Backup System
gcs-service-account.json
petwash-backup-*.tar.gz
firestore-restore/
.env
# Temporary setup files
setup-github.js
# Logs
*.log
npm-debug.log*
# Editor
.vscode/
.idea/
# Database
*.sqlite
*.sqlite3
# Cache
.cache/
tmp/
# Large test/benchmark files (exceed GitHub 100MB limit)
load-test-results.json
*.results.json
*-results.json
# Private keys — never commit
*.key
*.pem
petwash-wallet.key
# Secrets files — never commit
*.secrets*.txt
.secrets-for-nir-only.txt
# Session / auth artifacts — never commit
cookies.txt
test_cookies.txt
*_cookies.txt
tmp_login.txt
temp_response.txt
# Build / scan logs — never commit
build-output.txt
load-test-output.txt
