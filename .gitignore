node_modules
# dist/server and dist/public/assets contain baked-in env vars from build time.
# They must NOT be committed — Cloud Run builds its own dist during CI/CD.
# If you previously committed dist/ intentionally, remove it: git rm -r --cached dist/
dist/
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# GCS Backup System
gcs-service-account.json
petwash-backup-*.tar.gz
firestore-restore/
.env
# Temporary setup files
setup-github.js
# Logs
*.log
npm-debug.log*
# Editor
.vscode/
.idea/
# Database
*.sqlite
*.sqlite3
# Cache
.cache/
tmp/
# Large test/benchmark files (exceed GitHub 100MB limit)
load-test-results.json
*.results.json
*-results.json
# Private keys — never commit
*.key
*.pem
petwash-wallet.key
# Secrets files — never commit
*.secrets*.txt
.secrets-for-nir-only.txt
