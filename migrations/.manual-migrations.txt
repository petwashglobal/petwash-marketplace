# Manual migrations — DO NOT auto-apply
#
# Each line below is a filename in migrations/ that the auto-migration
# runner (scripts/apply-pending-migrations.ts) must NOT execute. The
# runner records each listed file in _petwash_migrations as
# `bootstrapped: true` and moves on.
#
# Use this when a migration depends on a table or subsystem that is
# managed outside the canonical Drizzle schema — for example, a
# deprecated subsystem whose tables were never created in this
# deployment.
#
# Comments start with `#`. Blank lines are ignored. One filename per line.
# Filenames must exactly match the file in migrations/ (no path prefix).

# 0009 ALTERs tranzila_transactions, which lives in shared/schema-tranzila.ts
# (out-of-band managed). The table does not exist in this deployment
# because Tranzila is being deprecated. Without this skip the runner
# fails 42P01 (undefined_table) and blocks every production deploy.
# Self-documented in the migration's own header comment:
#   "The tranzila_transactions table is managed outside the main
#    drizzle.config.ts ... Apply this migration manually or via a
#    dedicated migration runner before deploying code that uses
#    this column."
0009_tranzila_processor_auth_number.sql
