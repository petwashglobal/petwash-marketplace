# Manual migrations — DO NOT auto-apply
#
# Each line below is a filename in migrations/ that the auto-migration
# runner (scripts/apply-pending-migrations.ts) must NOT execute. The
# runner records each listed file in _petwash_migrations as
# `bootstrapped: true` and moves on.
#
# Use this when a migration depends on a table or subsystem that is
# managed outside the canonical Drizzle schema — for example, a
# deprecated subsystem whose tables were never created in this
# deployment.
#
# Comments start with `#`. Blank lines are ignored. One filename per line.
# Filenames must exactly match the file in migrations/ (no path prefix).

# 0009_tranzila_processor_auth_number.sql was DELETED 2026-05-24 as part
# of the Tranzila Option A kill. The orphan ALTER no longer exists in
# the migrations/ folder, so the runner won't see it and won't fail.
# This allowlist file remains in place for future manual migrations.

# 0124_concurrency_p0_uniques_2026_08_24.sql — SKIP.
# CREATE UNIQUE INDEX on wallet_accounts.user_id + digital_receipts hits
# duplicate-key errors on production DB (pre-existing dupes from earlier
# race-condition periods). Every deploy since 2026-08-24 fails on this
# migration, blocking 0125 (which adds the columns MonitoringWatchdog
# needs) from running. That leaves the OLD live revision throwing
# `column "last_requested_resubmission_at" does not exist` every 5 min
# and the /health/strict deploy gate rejecting the new revision.
# 0126 (below) reapplies 0125's ADD COLUMNs AND creates the same
# unique indexes wrapped in DO/EXCEPTION so pre-existing dupes only
# log a NOTICE instead of failing the deploy. Ops must dedupe by hand
# and then flip 0126's guards to strict on the next deploy window.
0124_concurrency_p0_uniques_2026_08_24.sql

# 0166_rebuild_catchup_declared_uniques.sql — SKIP (added 2026-09-18).
# The 20 UNIQUE indexes shared/schema*.ts declares that are absent from BOTH
# the migration series AND production (checked against the committed prod
# schema dump, 2026-07-03). Creating them is a real integrity change on live
# money tables (ledger_v2_*, refund_transactions, settlements,
# contractor_earnings), not a catch-up no-op, and CREATE UNIQUE INDEX fails
# with 23505 if any of those tables already holds duplicates — the same trap
# that made 0124 block every deploy on 2026-08-24. The file is committed so a
# database rebuilt from migrations/ alone still matches shared/schema*.ts (the
# PGlite rebuild gate applies every file directly, on empty tables). To clear
# it: Actions > "Declared-Unique Duplicate Scan (prod, read-only)" > Run
# workflow; if it is green, delete this line and the next deploy applies it.
0166_rebuild_catchup_declared_uniques.sql
