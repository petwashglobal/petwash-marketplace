<style>
    /* 1. IMPORT LUXURY TYPOGRAPHY */
    /* 'Cinzel' is a font often used by high-end fashion brands */
    @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@500;700;900&display=swap');

    /* 2. THE FLOATING CONTAINER */
    .luxury-2025-container {
        position: fixed;         /* Locks it to the screen */
        bottom: 30px;            /* Distance from bottom */
        left: 50%;
        transform: translateX(-50%); /* Centers it perfectly */
        z-index: 9999999;        /* Ensures it is the top layer */
        width: auto;
        max-width: 90%;
        pointer-events: none;    /* Allows clicks to pass through the empty area */
    }

    /* 3. THE BADGE (THE PILL SHAPE) */
    .luxury-badge-pill {
        pointer-events: auto;    /* Makes the badge itself clickable/interactive */
        background-color: #ffffff; /* Pure White */
        
        /* The Luxury Border Structure */
        border: 1px solid rgba(0,0,0,0.05);
        border-bottom: 2px solid #C5A059; /* The Signature Gold Line */
        
        /* Shape & Size */
        padding: 12px 28px;
        border-radius: 100px;
        
        /* Layout */
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 16px;
        
        /* 2025 Glass/Shadow Effect */
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08), 
                    0 2px 10px rgba(197, 160, 89, 0.2);
        
        /* Smooth Entrance Animation */
        opacity: 0;
        animation: luxurySlideUp 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        animation-delay: 0.5s; /* Waits a moment before appearing */
    }

    /* 4. THE GOLD ICON */
    .badge-icon svg {
        width: 24px;
        height: 24px;
        display: block;
        filter: drop-shadow(0 2px 4px rgba(197, 160, 89, 0.3));
    }

    /* 5. THE TEXT STACK */
    .badge-text-group {
        display: flex;
        flex-direction: column;
        text-align: left;
        line-height: 1.1;
    }

    /* Top Line: Brand Name */
    .brand-name {
        font-family: 'Cinzel', serif;
        font-size: 9px;
        color: #888888; /* Elegant Grey */
        letter-spacing: 2px; /* Wide "Fashion" Spacing */
        text-transform: uppercase;
        font-weight: 700;
        margin-bottom: 4px;
    }

    /* Bottom Line: Award Title */
    .award-title {
        font-family: 'Cinzel', serif;
        font-size: 13px;
        font-weight: 900; /* Bold */
        text-transform: uppercase;
        letter-spacing: 0.5px;
        
        /* THE "GOLD FRESH 2025" GRADIENT */
        background: linear-gradient(
            90deg, 
            #bf953f 0%, 
            #fcf6ba 40%, 
            #b38728 70%, 
            #fbf5b7 100%
        );
        background-size: 200% auto;
        color: #bf953f; /* Fallback */
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        
        /* Subtle Shimmer Animation */
        animation: goldShimmer 4s linear infinite;
    }

    /* ANIMATIONS */
    @keyframes luxurySlideUp {
        0% { opacity: 0; transform: translateY(40px); }
        100% { opacity: 1; transform: translateY(0); }
    }

    @keyframes goldShimmer {
        to { background-position: 200% center; }
    }

    /* MOBILE ADJUSTMENTS */
    @media (max-width: 600px) {
        .luxury-badge-pill { padding: 10px 20px; gap: 12px; }
        .brand-name { font-size: 8px; letter-spacing: 1.5px; }
        .award-title { font-size: 11px; }
        .badge-icon svg { width: 20px; height: 20px; }
    }
</style>

<div class="luxury-2025-container">
    <div class="luxury-badge-pill">
        
        <div class="badge-icon">
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M5 16L3 5L8.5 10L12 4L15.5 10L21 5L19 16H5ZM19 19C19 19.6 18.6 20 18 20H6C5.4 20 5 19.6 5 19V18H19V19Z" fill="#C5A059"/>
            </svg>
        </div>

        <div class="badge-text-group">
            <span class="brand-name">PET WASH™️ LTD</span>
            <span class="award-title">BEST PET HUB PLATFORMS 2025</span>
        </div>

    </div>
</div>
