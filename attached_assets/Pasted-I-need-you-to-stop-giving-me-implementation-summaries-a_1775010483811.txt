I need you to stop giving me implementation summaries and move to system proof + closure of critical gaps.

The architecture review is directionally good, but there are still a few serious items that stop this from being called production-ready.

Please work in this order.

1. Close the critical workflow gaps first

A. Applicant resubmission upload route
You already built the admin action that requests resubmission and creates a secure token, but the applicant still has no complete token-based backend path to upload the replacement files.
Need:
- POST /api/provider-onboarding/resubmit/:token
- validate token
- validate expiry
- lock request row
- store new selfie/document paths as a new file version
- update same application_id
- mark fulfilled_at
- move application back into submitted or processing
- write audit
- write message
- re-trigger async KYC

B. Resubmission token completion
fulfilled_at must be set on successful resubmission.
Right now the lifecycle is incomplete if the token remains open after use.

C. Expiry logic
expired state exists but is not actually enforced.
Need a scheduled job or deterministic backend rule that moves:
- pending_resubmission older than X days to expired
- optionally other stale states if business rules require it
Must write audit + monitoring event.

2. Prove the system with real evidence

Do not tell me "wired" or "implemented".
I want proof for these exact flows with real route responses and DB outcomes.

Flow 1
Clean auto-approve
Show:
- submit response
- final provider_applications row
- queue absence
- audit events
- message log
- email event

Flow 2
Pending review
Show:
- submit response
- queue row created
- support notification triggered
- applicant message created
- admin queue endpoint showing the record

Flow 3
Pending resubmission
Show:
- admin requests resubmission
- token created
- applicant resubmits with same application_id
- file version history updated
- fulfilled_at set
- audit trail shows request + upload + reprocessing

Flow 4
Manual approve
Show:
- admin approve route response
- queue completed
- reviewed_by / reviewed_at written
- applicant email sent
- audit event written

Flow 5
Manual reject
Show:
- admin reject route response
- queue completed
- rejection reason stored
- applicant email sent
- audit event written

Flow 6
KYC processing failure fallback
Force an async KYC failure and prove:
- application does not stay stuck in processing
- application moves to pending_review
- urgent queue entry created
- critical monitoring event written
- support notification sent

3. Prove race-condition safety

This is critical.

Approve and reject must be concurrency safe.
Show a real test where:
- two actions hit the same application
- first one succeeds
- second gets 409 CONCURRENT_ACTION_CONFLICT
- no double final state
- no duplicate audit decision

Also prove the same principle for:
- resubmission request called twice
- token reuse after fulfilled_at is set

4. Give exact architecture answers, not vague ones

I want a short but exact architecture note answering these:

A. Single source of truth for:
- application state
- communication thread
- review decision
- audit trail
- file history
- wallet

B. Full state machine
List every allowed transition from:
submitted
processing
pending_review
pending_resubmission
approved
rejected
expired
withdrawn

C. Idempotent actions
Which actions are intentionally idempotent and how:
- approve
- reject
- request resubmission
- resubmit upload
- message send
- assign reviewer

D. Locking strategy
Which DB operations use row locks or conditional status updates to prevent conflicting actions.

E. External failure handling
What happens when:
- KYC engine fails
- storage fetch fails
- email send fails

F. Schema truth
List which fields exist only in raw SQL today and which are already synced into the ORM schema.
I do not want schema drift hidden.

5. Tighten role separation

I need exact permission separation, not a general "admin can do it".

Show the exact rule set for:
- support
- management
- admin
- super_admin
- provider
- public loyalty user

In particular:
- who can review applications
- who can only view analytics
- who can send applicant messages
- who can approve/reject
- who can request resubmission

Management and support must not be blurred.

6. File review UX still needs completion

I need proper internal review usability, not only file paths.

Need:
- image preview
- PDF preview
- zoom / fullscreen
- open original
- download original
- version history
- compare previous vs latest resubmission

This must work on:
- small iPhone
- Android
- iPad
- desktop

7. Monitoring rules must become real

Do not leave monitoring as just a table.

Define and implement thresholds for:
- processing stuck more than X minutes
- pending_review overdue more than X hours
- pending_resubmission overdue more than X days
- email failure spike
- KYC failure spike
- rejection spike

Each one must produce a provider_workflow_events record and a clear severity.

8. Final deliverable format

Reply with:
1. critical gaps closed
2. exact files changed
3. exact routes added or changed
4. proof output for all 6 flows
5. proof of race-condition handling
6. permission matrix
7. state transition matrix
8. monitoring thresholds
9. schema sync status
10. remaining gaps, if any

This is close, but not yet closed.
Close the operational gaps first, then prove the system.