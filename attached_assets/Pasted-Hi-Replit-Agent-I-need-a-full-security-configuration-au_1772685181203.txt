Hi Replit Agent — I need a full security + configuration audit and concrete fixes. Please answer each item with: (1) exact file path(s), (2) exact settings screen location, (3) what to change, (4) how to verify.

A) TWILIO / FRAUD PREVENTION
1) Confirm where Twilio credentials are stored (Replit Secrets vs env files vs code). List every place they appear. Ensure nothing is committed to Git.
2) Show all outbound SMS/call code paths: which endpoints trigger messages/calls? Provide file paths + routes.
3) Add “hard stop” protections:
   - per-IP rate limiting on any endpoint that can send SMS/calls
   - per-user rate limiting (if logged in)
   - daily cap (messages/day) enforced in code
   - allowlist of destination country codes (block everything else)
4) Confirm if any unauthenticated endpoint can trigger Twilio. If yes, fix it (auth + captcha + rate limit).
5) Add monitoring: log every send attempt with userId/ip/UA, and add alert triggers.

B) GOOGLE reCAPTCHA / PROJECT MISMATCH
6) Our verification logs show Enterprise PERMISSION_DENIED “project does not own this recaptcha key”. Identify:
   - which GCP project the site key belongs to
   - which project the backend is calling
   - what env vars are used (names + where read in code)
7) Fix so backend verifies tokens using the correct project. Provide the exact env var(s) needed and where to set them.
8) Confirm frontend is generating tokens from the same site key the backend expects.
9) Verification must be fail-closed (block requests if verification fails) for sensitive endpoints.

C) GOOGLE CLOUD STORAGE / “REVISIONS” / CLEANUP
10) User wants to “clear old cached versions”. Identify what they actually mean in this stack:
   - Cloud Storage buckets (which ones exist, sizes)
   - Firebase Hosting versions / releases
   - Cloud Run revisions (if used)
   - Artifact Registry images (if used)
11) Provide step-by-step cleanup instructions for each:
   - delete unused Cloud Run revisions (or reduce)
   - delete old container images in Artifact Registry with retention policy
   - delete old build artifacts if stored in buckets
   - confirm what is safe to delete without breaking prod
12) Add retention policies (so it stays clean automatically).

D) GITHUB / BUILD ARTIFACTS / ACTIONS
13) Identify whether GitHub Actions is storing artifacts/packages/releases that are consuming space. Provide where to find them in GitHub UI and which to delete.
14) If Container Registry/GHCR used, set retention policy and remove old images.

E) SECRETS / ENV VARS SANITY CHECK
15) Produce a complete list of required secrets/env vars for production (names only, no values), grouped by service.
16) Verify there are no “wrong-format” secrets (e.g., API key placed where JSON is expected). Add validation on startup to fail fast with clear error.

F) ADMIN ACCESS SETUP
17) Confirm SUPER_ADMIN_EMAILS is read correctly (comma-separated) and show where it is used. Ensure no hardcoded admin emails remain in source.

G) DEPENDENCIES
18) Do NOT upgrade major breaking versions automatically. Only apply safe patch/minor updates, and show the exact PR/commit list.

Finally: give me a “Verification Checklist” with commands + expected output/log lines to prove everything is fixed.