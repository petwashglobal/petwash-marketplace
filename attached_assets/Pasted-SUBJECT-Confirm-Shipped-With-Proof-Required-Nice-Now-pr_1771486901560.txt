SUBJECT: Confirm “Shipped” With Proof (Required)

Nice. Now prove it. No more narration.

1) PRs + commits
- Send PR links (or commit hashes) for each task:
  T1 score cap
  T2 auth endpoints 410
  T3 DB dispatch poller
  T4 Postgres KYC rate limiter
  T5 Places proxy endpoints
  T6 registration hardening
  T7 Sheets idempotency + recon
  T8 docs honesty

2) Migrations evidence
- Paste the SQL migrations you ran:
  - job_offers nextWaveAt (and any other columns)
  - kyc_rate_limits table + constraints
  - sheets idempotency table + constraints
- Confirm migration ran on production DB (not only local).
- Provide “SELECT column_name” output for the new columns.

3) Production smoke tests (video proof)
Send 3 short screen recordings (max 2 min each) on the real production domain:
A) New Loyalty Member signup
- email signup
- phone verify
- address autocomplete shows suggestions
- submit success
- show dashboard loaded

B) New Provider signup
- multi-step
- address suggestions
- mobile verify
- submit success
- show provider record created

C) Google sign-in + reCAPTCHA
- show reCAPTCHA token being requested and verified (network tab or server log)
- google sign-in completes, redirect works, user logged in

4) Logs and trace IDs
For each of the 3 flows above:
- Trace ID
- Server log lines for:
  - recaptcha verification result
  - profile creation result (userId + profileId)
  - any errors, full error string
- DB row IDs created (users table, profiles, provider_application, etc.)

5) Places proxy verification
- Confirm frontend is using backend proxy, not browser key:
  - /api/places/autocomplete and /api/places/details
- Confirm Google API key is server-side only and restricted
- Confirm address suggestions work on iOS Safari and Android Chrome

6) Dispatch reliability proof
- Show dispatch poller running after a server restart:
  - Create a job
  - Restart server
  - Poller resumes and continues waves
- Show DB state transitions:
  - offer created with wave1
  - nextWaveAt set
  - wave2 created
  - wave3 created or job expired
- Show what happens if job expires:
  - customer gets a notification or visible UI status (if not implemented, say “not done”)

7) KYC rate limiter proof
- Provide the exact limits enforced (per hour/day) and keys used
- Demonstrate with requests until blocked
- Show DB rows in kyc_rate_limits table

8) Sheets idempotency and recon proof
- Demonstrate:
  - same event submitted twice does NOT duplicate row
  - retry queue works
- Provide recon endpoint output for a sample day:
  - Postgres count
  - Sheets count
  - mismatch list

Only after this evidence is delivered, we call it shipped.