Good. Phase 12.24 is accepted.

The business logic is correct and this is the right way to build it.

Most important point:
Do not touch or overload the existing governance policy_versions table.
That table already belongs to another layer.
Using separate rollout tables for this phase is the correct decision because rollout discipline is not the same thing as governance execution history.

What I expect from this phase:
1. No duplicate engines
   - Reuse 12.22 outcome measurement
   - Reuse 12.23 policy feedback logic
   - Do not invent a second scoring path anywhere

2. Safe change control only
   - draft
   - activate
   - rollout by scope
   - evaluate
   - rollback
   This is the exact chain that must exist

3. Scope must be real and explicit
   - global
   - franchise
   - station
   - ownership type
   No vague targeting and no hidden side effects

4. Evaluation must stay honest
   - if there is no baseline, say no baseline
   - if there are no resolved measured cases, do not show fake 0% success
   - if sample size is weak, surface that clearly

5. Rollback must be immediate and auditable
   - not just a UI button
   - must update rollout state properly
   - must preserve the history

6. UI should help decision makers, not developers
   I want:
   - clear policy key
   - version number
   - current active version
   - rollout scope
   - rollout status
   - sample size
   - success rate
   - margin delta
   - friction delta
   - rollback path
   No clutter and no technical noise

7. Do not call anything “successful” unless 12.22 measured it
   Measured outcomes only.
   Nothing assumed.

Why this phase matters:
12.23 tells us what should change.
12.24 is the control layer that lets us change rules safely without damaging operations.
If this layer is wrong, the whole learning loop becomes dangerous.
If this layer is right, the platform can improve itself without chaos.

So yes, go ahead exactly this way.

Before closing, I want proof on these points:
- creating a new draft version works
- activating a draft archives the previous active one
- rollout cannot be created from a non-active config
- rollout evaluation reads 12.22 correctly
- rollback works and leaves an audit trail
- weak sample sizes are shown honestly, not disguised

Once those pass, Phase 12.24 can be closed.