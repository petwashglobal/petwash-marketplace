We have enough evidence now from repeated independent audits.

This is no longer bug fixing.
This is production integrity remediation.

Stop all non-critical feature work and fix in this exact order.

P0 - immediate

1. Fix all SQL injection vulnerabilities in server/routes/prestige-pass.ts
- replace every raw interpolated query with parameterized queries only
- no exceptions
- provide exact file lines changed

2. Standardize admin authorization
- remove all hardcoded admin emails
- remove all hardcoded Firebase UID authority
- use one role-based admin auth model only
- apply timing-safe secret comparison everywhere
- enforce second factor for admin access

3. Consolidate WebAuthn / passkeys
- choose one implementation only
- remove or disable the duplicate implementation
- store challenge server-side
- secure authenticate/verify properly
- implement real attestation validation
- choose one credential store as the source of truth

4. Make all payment flows atomic
- add idempotency keys
- wrap payment + escrow + booking update in DB transactions
- no partial success allowed
- if escrow creation fails, the flow must fail hard

5. Build one shared registerUserPostAuth() flow
It must always create and/or verify:
- Postgres user
- wallet
- loyalty
- baseline profile
This must be used by email, phone, Google, Apple, Facebook, Instagram, TikTok.

6. Fix the broken signup blockers
- phone OTP dead-end
- verify-email missing route/page
- Facebook missing server callback
- Apple returning-user lookup issue
- OAuth provider intent loss
- duplicate redirect result handling

7. K9000 truthfulness
- if no real machine command is sent, do not represent the wash as started
- every audit/event row must explicitly record whether the hardware command was actually sent

P1 - this week

8. Connect dispute resolution to real escrow action
- refund or release must happen as part of dispute resolution

9. Encrypt Google Drive backups before upload
- add alerting on backup failure
- no silent backup failure

10. Remove server-side use of VITE_* secrets
- especially Google Maps style exposure
- use server-only env vars

11. Make CAPTCHA blocking on OTP send
- if CAPTCHA fails, reject the request

12. Add strict upload controls
- file size limit on careers.ts
- review all upload endpoints

13. Reduce signed URL lifetime
- no 1-year signed URLs for sensitive documents

14. Clean route mounting conflicts
- review duplicate /api/k9000 mounting
- narrow walkMyPet route mounting
- disable deprecated reachable routes

P2 - next

15. Unify role source of truth
16. Unify provider source of truth
17. Unify booking state model
18. Remove dead/deprecated auth and booking code paths
19. Add regression tests for auth, signup, payment, escrow, dispute, admin RBAC, and provider onboarding
20. Run a compliance pass on invoices, VAT, provider verification, and trust/safety flows

For each completed item I want:
- exact file changed
- exact commit
- before behavior
- after behavior
- proof of fix
- whether migration, rotation, or data repair is needed