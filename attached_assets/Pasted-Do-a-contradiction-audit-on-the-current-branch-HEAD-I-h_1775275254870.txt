Do a contradiction audit on the current branch HEAD.

I have two sets of findings:
1. earlier claims that some issues were fixed
2. new findings saying several of those same areas are still open

Your job is to resolve contradictions with exact proof from current code only.
Do not trust old summaries, commits, or prior chat claims.
Verify the live current code path at HEAD.

For every item below, return:
- status: FIXED / PARTIAL / OPEN / FALSE POSITIVE
- exact file and line range
- exact route / service / function
- exact current code snippet
- why previous audit may have been wrong
- exploit path if still open
- exact fix if still open

CONTRADICTION LIST TO RE-VERIFY

1. Demo payment fallback
- Check if any payment service still silently falls back to demo or fake success when NAYAX keys are missing
- Verify:
  - server/services/NayaxSparkService.ts
  - server/nayaxService.ts
  - server/nayaxFirestoreService.ts
  - server/routes.ts express checkout
  - any other Nayax or payment services
- Need exact answer:
  - Can a real user still complete any paid flow without a real charge?
  - Which exact routes and services allow that today?

2. Booking completion ownership
- Re-verify:
  - server/routes/bookings.ts
  - server/routes/marketplace-bookings.ts
  - server/routes/super-app-bookings.ts
  - any /complete, /confirm, /cancel, /transition route
- Need exact answer:
  - Can any authenticated user still complete or cancel another user’s booking?
  - Are role values still derived from req.body or req.query anywhere?

3. Background check truth
- Re-verify:
  - provider-onboarding.ts
  - contractor-onboarding.ts
  - provider-applications.ts
  - any AdminProviderReviewService or review service
- Need exact answer:
  - Is backgroundCheckStatus ever being written as passed without a real external check?
  - Is police check hard-required before approval?
  - Can a provider still become active without real criminal screening?

4. Auto-approval of providers
- Re-verify:
  - provider-onboarding.ts around KYC scoring / auto-approval logic
  - any system-reviewed, system-kyc2026, or automatic approval path
- Need exact answer:
  - Can the system activate a provider without human review?
  - Which thresholds or conditions trigger that?

5. Hardcoded super admin values
- Search entire repo for:
  - nirhadad1@gmail.com
  - nir.h@petwash.co.il
  - vdiboz7IrUQEm2RbdO7VZLkBu552
  - SUPER_ADMIN_UID
  - hardcoded admin email arrays
- Need exact answer:
  - list every remaining hardcoded admin email or UID
  - state whether each is only display text, logic, or actual authorization logic

6. Timing-safe admin secret comparisons
- Search every route for:
  - x-admin-secret
  - x-admin-key
  - ADMIN_SECRET
  - ADMIN_API_KEY
  - === process.env
  - !== process.env
- Need exact answer:
  - list every remaining non-timing-safe compare
  - distinguish fixed vs still open

7. Wallet split-brain
- Re-verify:
  - wallet_accounts
  - petwash_pass_accounts
  - wallet_ledger_entries
  - any cached balance fields
  - all redeem and credit paths
- Need exact answer:
  - are there still two live wallet/balance authorities?
  - can the same funds be spent from two places?
  - is ledger immutable at DB level or only by convention?

8. Gift card / e-gift atomic redemption
- Re-verify:
  - gift-cards.ts
  - e-gift related routes
  - voucher redeem paths
- Need exact answer:
  - is redeemed flag updated atomically in one SQL statement?
  - can two concurrent requests redeem the same card twice?

9. Pass / package redeem amount integrity
- Re-verify:
  - pass-redeem.ts
  - unified-vouchers.ts
  - nayaxFirestoreService.ts
  - any package redeem route
- Need exact answer:
  - is amountIls or any redeem amount still trusted from client input?
  - is machine binding enforced or only warned in logs?
  - can a user redeem higher value than the actual service price?

10. Webhook replay protection
- Re-verify:
  - nayax-webhooks.ts
  - nayax-monyx-events.ts
  - nayax-payments.ts
- Need exact answer:
  - is dedup persistent or only memory / optional Redis?
  - does replay protection survive restart and multi-instance deployment?
  - is 24h retention used?

11. Biometric and document encryption
- Re-verify:
  - provider-onboarding.ts uploads
  - contractor-documents.ts
  - biometricStorage.ts
  - DocumentEncryption usage
- Need exact answer:
  - are selfie, face, ID, passport, and license files encrypted before storage?
  - or only bank fields?
  - if bucket is compromised, are raw images readable?

12. VITE env vars used server-side
- Search whole backend for process.env.VITE_
- Need exact answer:
  - list every backend file still reading VITE_* env vars
  - explain exposure risk and exact replacement

13. ITA / invoice route auth
- Re-verify:
  - contractor-invoices.ts
  - ita-api.ts
  - Israeli invoice generators
- Need exact answer:
  - which invoice routes lack requireAuth or ownership checks?
  - can one user fetch another user’s invoice by ID?

14. Deprecated legacy routes still active
- Re-verify:
  - octopus-engine.ts
  - any deprecated v1 booking routes
- Need exact answer:
  - which deprecated routes still execute business logic instead of returning 410?
  - should they be killed immediately?

15. Slot locking and double booking
- Re-verify:
  - booking-requests.ts
  - marketplace-bookings.ts
  - provider slots / availability logic
- Need exact answer:
  - is there an actual atomic slot lock?
  - can two customers book same slot concurrently?
  - are failed / expired / cancelled bookings releasing slots correctly in all systems?

16. reCAPTCHA fail-open on OTP
- Re-verify:
  - publicAuthRoutes.ts
  - OTP services
- Need exact answer:
  - does captcha failure still allow SMS send?
  - show exact branch and response behavior

17. Role-from-query / role-from-body trust
- Search all repo for:
  - req.query.role
  - req.body.role
- Need exact answer:
  - list every route where role is caller-controlled
  - say whether it influences authorization, state machine, or UI only

18. Provider browse / stats privacy leaks
- Re-verify:
  - provider-trust.ts
  - provider-intake.ts
  - control-panel.ts
  - providers.ts
- Need exact answer:
  - which endpoints expose provider names, statuses, trust data, or PII without auth?

19. Database-level protections
- Re-verify schema and migrations for:
  - enum constraints
  - append-only ledger protection
  - foreign keys between provider tables
  - unique constraints for redemption / processed webhooks / cases
- Need exact answer:
  - which business rules exist only in app code and not in the DB?

20. True top 20 open risks at HEAD
- After contradiction audit, give the actual current top 20 open risks only
- No duplicates
- No already-fixed items
- Rank by exploitability + production damage

MANDATORY OUTPUT FORMAT

Section 1: Contradiction Resolution Table
Columns:
- Item
- Previous claim
- Current truth at HEAD
- Status
- Exact proof

Section 2: Still Open and Exploitable Now
Only items that are truly still open

Section 3: False Positives
Items that sounded bad but are no longer true

Section 4: Immediate Kill Switch List
Exact routes or features that should return 410 or 503 today

Section 5: 1-Day Patch List
Only fixes small enough to land safely today

Section 6: Architecture Refactor List
Only structural fixes requiring more than one day

Important:
- Use direct code proof only
- No speculation
- No trust in old chats
- No “likely”
- If uncertain, say uncertain and show why