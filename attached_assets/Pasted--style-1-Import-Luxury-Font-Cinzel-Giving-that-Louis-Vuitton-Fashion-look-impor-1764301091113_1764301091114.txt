<style>
    /* 1. Import Luxury Font (Cinzel) - Giving that Louis Vuitton/Fashion look */
    @import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@500;700&display=swap');

    /* 2. Container to float at the bottom of the screen */
    .luxury-award-container {
        position: fixed;
        bottom: 25px;       /* Spacing from bottom */
        left: 50%;
        transform: translateX(-50%); /* Center align */
        z-index: 999999;    /* Ensure it sits on top of everything */
        width: auto;
        max-width: 95%;
        pointer-events: none; /* Allows clicking through the empty space around it */
    }

    /* 3. The Badge Itself (The "Pill") */
    .luxury-badge {
        pointer-events: auto; /* Re-enable clicking on the badge itself */
        background-color: #ffffff;
        
        /* Subtle Borders */
        border: 1px solid #f2f2f2;
        border-bottom: 2px solid #C5A059; /* The Gold Foundation Line */
        
        /* Shape & Spacing */
        padding: 10px 24px;
        border-radius: 100px;
        
        /* Layout */
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 15px;
        
        /* Luxury Shadow (Diffused Gold/Grey) */
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05), 
                    0 4px 10px rgba(197, 160, 89, 0.1);

        /* Entrance Animation */
        animation: luxuryFloatUp 1.2s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
        opacity: 0; /* Start invisible for animation */
    }

    /* 4. The Icon (Crown) */
    .badge-icon svg {
        display: block;
        width: 22px;
        height: 22px;
    }

    /* 5. Text Column */
    .badge-content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        text-align: left;
    }

    /* 6. Top Text: PET WASH LTD */
    .badge-company {
        font-family: 'Cinzel', serif;
        font-size: 9px;
        letter-spacing: 2.5px; /* Wide "Fashion Label" Spacing */
        color: #888888;
        font-weight: 700;
        text-transform: uppercase;
        margin-bottom: 3px;
        line-height: 1;
    }

    /* 7. Bottom Text: AWARD TITLE */
    .badge-title {
        font-family: 'Cinzel', serif;
        font-size: 13px;
        letter-spacing: 1px;
        font-weight: 700;
        text-transform: uppercase;
        line-height: 1;
        
        /* Gold Metallic Gradient Effect on Text */
        background: linear-gradient(90deg, #b88a4d 0%, #e8c87e 50%, #b88a4d 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        color: #C5A059; /* Fallback color */
    }

    /* Animation Keyframes */
    @keyframes luxuryFloatUp {
        0% { opacity: 0; transform: translateY(30px); }
        100% { opacity: 1; transform: translateY(0); }
    }

    /* Mobile Responsiveness */
    @media (max-width: 480px) {
        .luxury-badge { padding: 10px 18px; gap: 10px; }
        .badge-company { font-size: 8px; letter-spacing: 1.5px; }
        .badge-title { font-size: 10px; }
        .badge-icon svg { width: 18px; height: 18px; }
    }
</style>

<div class="luxury-award-container">
    <div class="luxury-badge">
        <div class="badge-icon">
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M5 16L3 5L8.5 10L12 4L15.5 10L21 5L19 16H5ZM19 19C19 19.6 18.6 20 18 20H6C5.4 20 5 19.6 5 19V18H19V19Z" fill="#C5A059"/>
            </svg>
        </div>
        
        <div class="badge-content">
            <span class="badge-company">PET WASH™️ LTD</span>
            <span class="badge-title">BEST PET HUB PLATFORM 2025</span>
        </div>
    </div>
</div>

