🔥 PETWASH FULL FIX MASTER INSTRUCTION (SEND THIS TO REPLIT)

You are fixing a production system. No partial fixes. No assumptions. Fix ALL issues below in one pass.

---

1. GOOGLE ADDRESS AUTOCOMPLETE NOT WORKING

Problem:
- Address suggestions inconsistent across devices
- Sometimes not showing at all (mobile especially)

Fix:

- Ensure APIs enabled:
  - Maps JavaScript API
  - Places API

- Fix API key restrictions:
  - Allow: https://petwash.co.il/*
  - Remove overly strict referrer limits

- Load script EARLY (not lazy loaded):
  <script src="https://maps.googleapis.com/maps/api/js?key=API_KEY&libraries=places"></script>

- Fix RTL input issue:
  - Input must be dir="ltr" internally
  - UI remains RTL

- Fix dropdown visibility:
  .pac-container { z-index: 9999 !important; }

---

2. PROVIDER ONBOARDING FAILING

Problem:
- “Become provider” fails silently
- Form submits but does not complete

Root cause likely:
- Missing authentication credentials
- Backend rejecting request (401)

Fix:

- ALL fetch/axios requests MUST include:
  credentials: 'include'

- Add full logging:
  console.log(response.status)
  console.log(await response.text())

- Backend:
  - Verify authenticated user (Firebase UID exists)
  - Validate required fields:
    - phone
    - ID number
    - address
  - Return explicit errors (NO silent fail)

---

3. PAW FINDER EMPTY STATE (CRITICAL UX BUG)

Problem:
- Page shows empty state instead of demo content

Fix:

IF no posts in DB:
- Inject STATIC demo data
- Always show:
  - 2 “Lost” pets
  - 2 “Found” pets
  - Map markers

Rules:
- Do NOT wait for search
- Do NOT depend on DB
- Demo content must render instantly

---

4. MOBILE LAYOUT BROKEN

Problems:
- Keyboard overlaps inputs
- Layout stretches / breaks
- Floating buttons overlap UI

Fix:

- Add viewport:
  <meta name="viewport" content="width=device-width, initial-scale=1">

- Remove fixed heights:
  Use min-height, flex, responsive units

- Add bottom spacing for forms:
  padding-bottom: 120px;

- On input focus:
  element.scrollIntoView({ behavior: 'smooth', block: 'center' });

- Fix floating buttons:
  position: fixed;
  bottom: calc(16px + env(safe-area-inset-bottom));

---

5. BRANDING RULE - TM POSITION

MANDATORY:

Correct:
Pet Wash™

Wrong:
™Pet Wash
PetWash™
Pet Wash ™

Fix RTL issues:

.brand {
  direction: ltr;
  unicode-bidi: isolate;
}

Ensure ALL instances follow:
Pet Wash™

---

6. TEXT BREAKING / COLUMN BUG

Problem:
- Words breaking incorrectly in columns

Fix:

Remove:
word-break: break-all;

Use:
word-break: normal;
overflow-wrap: break-word;

Avoid flex shrink issues:
min-width: 0;

---

7. GENERAL FRONTEND STABILITY

- NO silent errors anywhere
- Every API must return visible error if fails
- All async flows must have loading + error state
- Ensure consistent RTL layout across components

---

FINAL RULE:

This is a production system.

Do NOT:
- Skip validation
- Hide errors
- Assume success

Everything must:
- Log clearly
- Fail visibly
- Work on mobile first

---

END OF TASK