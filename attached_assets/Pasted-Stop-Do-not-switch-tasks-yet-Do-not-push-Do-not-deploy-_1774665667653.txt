Stop.

Do not switch tasks yet.
Do not push.
Do not deploy.

We are still finishing:
Compliance-safe KYC retention system

Rules:
- no partial push
- no partial deploy
- no “almost ready”
- no switching tasks
- only one final push after full completion and approval

Current required flow:
1. finish all coding locally
2. finish all verification locally
3. close every blocker
4. return final strict report
5. wait for approval
6. then push once
7. then we test live once

Before approval, confirm clearly:
Does providers.kyc_deletion_completed_at update for BOTH storage paths:
- Firebase Storage (/api/kyc/upload)
- GCS (/api/documents/upload)

Return only when fully complete in required format.

---

NEXT TASK (DO NOT START UNTIL KYC IS CLOSED)

TASK:
SMS OTP verification + iPhone autofill

OBJECTIVE:
Full SMS verification working for Israel and Australia with correct OTP autofill behavior on iPhone and Android.

TEST NUMBERS:
+972549833355
+61419773360

IMPORTANT:
I cannot accept a custom keypad-only OTP.
A real input field is mandatory for autofill.

REQUIREMENTS:

1. SMS DELIVERY
- must work for Israel and Australia
- numbers must be handled in E.164 format

2. OTP INPUT (CRITICAL)
Must include a real input field:

<input
  type="text"
  inputmode="numeric"
  autocomplete="one-time-code"
  maxlength="6"
  pattern="[0-9]{6}"
/>

- this is REQUIRED for iPhone autofill
- no input = no autofill

3. UI
- you may keep luxury OTP boxes
- but they MUST be synced from the real input
- input must be focused automatically

4. SMS FORMAT (CRITICAL)

SMS must be EXACTLY like this:

Your PetWash verification code is 123456.

@pet-wash-ltd.replit.app #123456

- required for autofill detection
- required for Android compatibility

5. FLOW REQUIREMENTS
- send code
- verify code
- resend code
- expiry handling
- invalid code handling
- success flow

6. BACKEND
- use existing Twilio service
- ensure Israel + Australia enabled
- no duplicate sends
- rate limit protection

7. FRONTEND
- real input field
- synced visual boxes
- supports:
  - typing
  - autofill
  - paste
  - manual entry

8. TESTING (LOCAL FIRST)

Must test:
- Israeli number
- Australian number
- iPhone autofill
- Android autofill (if supported)
- manual input fallback

9. EDGE CASES
- wrong code
- expired code
- resend limit
- SMS delay
- network failure

RETURN FORMAT (AFTER BUILD):

TASK:
CURRENT OTP FLOW:
ROOT CAUSE:
FILES TO CHANGE:
SMS FORMAT:
INPUT STRATEGY:
COUNTRY TEST PLAN:
EDGE CASES:
RISK:
READY FOR BUILD:

Do not write code yet.
Do not push.
Do not deploy.

Finish KYC first.
Then start this task cleanly.