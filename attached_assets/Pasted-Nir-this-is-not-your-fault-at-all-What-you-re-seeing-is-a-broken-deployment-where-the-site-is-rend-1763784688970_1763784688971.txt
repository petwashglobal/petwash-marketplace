Nir, this is not your fault at all. What you’re seeing is a broken deployment where the site is rendering duplicate components, missing images, wrong layout, and repeated headers.
This is 100 percent a developer-side bug, not DNS, not your phone, not caching.

I’ll give you a clean, precise explanation + a ready message you can send him so he fixes it immediately and correctly.

⸻

🔍 What’s wrong (clear list)

1. Duplicate header / duplicate social icons

You see:
	•	Two rows of X, IG, PetWash icons
	•	Two language selectors
	•	Two login buttons
	•	Two menu icons

This means the page is rendering your <Header> component twice.

2. Missing hero image

The big blue “Get Started” block should have your hero image. Instead you see:
	•	The blue broken image icon
This means:
	•	The hero image is not in /public or
	•	The import path is wrong or
	•	Replit asset folder was replaced or
	•	The build folder has old content

3. Components overlap (layout breaking)

You scroll and see:
	•	“Advanced Technology” repeated
	•	Cards duplicated
	•	Buttons not aligned
This means:
	•	Homepage is rendering the same section twice or
	•	The developer merged files incorrectly

4. Wrong homepage content

This is NOT the homepage we built together.
Your real homepage includes:
	•	Hero section with image
	•	Clean luxury layout
	•	PetWash story section
	•	K9000 Israel features
	•	Buttons with actions
None of these are there.

5. Floating buttons overlapping everything

The WhatsApp and chat icons are sitting on top of content because:
	•	The z-index or spacing is wrong
	•	Or the component is placed inside the wrong container

⸻

📌 This is a code merge issue, not caching and not DNS

Specifically:

The developer likely:
	•	Copied the homepage twice into the component
	•	Rendered two headers
	•	Left duplicate JSX inside the layout file
	•	Or merged two pull requests incorrectly
	•	Or committed a broken version to Replit before deploy

⸻

💣 Immediate fix required (send this to the dev)

Copy and send him this EXACT message:

⸻

MESSAGE FOR DEVELOPER