Good. This is the right Saved Providers implementation.

I accept this as completed.

What is now good:
- saved_providers is the source of truth
- GET /api/saved-providers is enriched enough for real UI use
- ProviderCard now has a real save heart
- ProviderProfilePage stays in sync through the same query key
- CustomerFavourites shortlisted tab is now API-backed
- the same provider can be saved/unsaved from multiple surfaces and stay in sync

Now I want the final hardening and proof pass for this feature.

1. Proof I want next
Please send:
- one GET /api/saved-providers response example with enriched fields
- one successful save proof
- one successful unsave proof
- one proof that duplicate save does not create a second row
- one screenshot of ProviderCard with saved heart
- one screenshot of ProviderProfilePage with saved heart
- one screenshot of CustomerFavourites shortlisted tab populated from API

2. Hardening checks
Please confirm:
- save requires auth
- unsave requires auth
- GET /api/saved-providers is scoped to the current user only
- nonexistent provider ids are rejected cleanly
- a saved provider removed from the platform degrades gracefully in the UI
- image fallback works when profilePicUrl is null
- toggle rollback works on API failure
- no localStorage shortlist logic remains active anywhere

3. UX polish I want
Please improve:
- saved heart hover / pressed state
- empty state for shortlisted tab
- loading skeleton for shortlisted tab
- better CTA wording than generic "Book" if needed
- optional "View profile" and "Book now" split later if useful

4. Next logical build order
Now do this in order:
A. Saved Providers proof and hardening finish
B. Rebook flow maturity from past bookings and saved providers
C. Customer booking management polish
D. Notification center and booking timeline maturity

5. Important architecture rule
Saved providers should now always use:
- source of truth: saved_providers
- read path: /api/saved-providers
- write path: POST/DELETE /api/saved-providers/:id
- shared query key: /api/saved-providers

Do not create another favourites system.