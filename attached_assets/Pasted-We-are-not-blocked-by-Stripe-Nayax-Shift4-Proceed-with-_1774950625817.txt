We are not blocked by Stripe / Nayax / Shift4.

Proceed with the following fixes immediately. These are required to make the system production-safe.

---

## 1. App Check (CRITICAL)

Activate Firebase App Check in production.

- Set: VITE_FIREBASE_APPCHECK_SITE_KEY
- Enforce App Check on backend verification
- Confirm login works on:
  - iPhone Safari
  - iPhone Chrome
  - Android Chrome

System must NOT run in fail-open mode.

---

## 2. Phone Uniqueness Error Handling

DB constraint exists. Now fix API behavior.

- Catch PostgreSQL error code 23505
- Return HTTP 409 (not 500)
- Message: "Phone number already in use"

Apply to:
- signup route
- profile update route

---

## 3. Payout System Activation (CRITICAL)

System exists but not used correctly.

- Use: POST /api/treasury/batches
- Provide valid settlement_ids from station_settlements

Verify:
- payout_batch_items is populated
- batch status updates correctly
- failures are tracked

Provide proof:
- 1 successful batch
- 1 failed case (simulate if needed)

---

## 4. Reconciliation Stress Test (CRITICAL)

Do NOT rely on “0 discrepancies”.

Simulate:

- duplicate Nayax transaction ID
- missing settlement
- mismatched amount (small delta)
- delayed payout

Verify:
- detected in pw_reconciliation_reports
- correct severity classification

---

## 5. Mobile & Social Auth Verification

Test real devices:

- iPhone Safari
- iPhone Chrome
- Android Chrome

Flows:
- email signup
- OTP
- Google
- Facebook
- Instagram
- TikTok

Fix:
- iosAuthHandler detection issues
- redirect fallback reliability

---

## 6. Deprecated Auth Endpoints

Confirm no active usage of:

- /api/auth/register
- /api/auth/login

If used:
- log requests
- block or redirect cleanly

---

## 7. Voucher / E-Gift Canonical Logic

Define single source of truth:

- wallet_accounts.egift_balance_cents = balance
- egift_events = ledger

Clarify:
- all other voucher tables = legacy or derived

Provide one query for:
"total outstanding gift liability"

---

## 8. Localization Completion

Add IP mapping:

- FR → fr
- ES → es
- RU → ru
- AE → ar

Priority remains:
user > browser > IP

---

## 9. Compliance Finalization (Israel 2026)

Update:

- Accessibility Statement → explicitly mention "ת״י 5568"
- Privacy Policy → include named DPO (person or role)

---

## 10. Intervention Snapshot Enforcement (CRITICAL)

Ensure ALL intervention cases include:

- snapshot_margin_pct
- snapshot_friction_pct
- snapshot_reserve_risk
- snapshot_failure_rate

Apply to:
- manual cases
- auto-generated cases

If missing → BLOCK case creation

---

## OUTPUT REQUIRED

For each item above return:

- DONE / PARTIAL / MISSING
- exact file(s)
- exact table(s)
- exact route(s)
- proof (logs, DB rows, or screenshots)

No general answers.

---

Execute in this order:

1. App Check
2. Payout batch
3. Reconciliation stress
4. Auth verification
5. Snapshot enforcement
6. Remaining items

---

This will make the system operationally safe before payment integrations.