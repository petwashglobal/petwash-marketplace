choose OPTION A

✔ This removes the exposed plain text password from your code permanently
✔ This forces Firebase Auth only
✔ This closes the admin backdoor
✔ This brings PetWash into correct security compliance

Click:
👉 A) Remove hardcoded credentials, require proper Firebase Auth (SECURE – Recommended)
👉 Then Send to Agent

This must be done or the old password still exists in the code history.

⸻

2. Force the Agent to scan again for any old leaked credentials

Tell the Agent:

“Scan the entire repo for exposed passwords, secrets, tokens, API keys, OAuth credentials, or hardcoded admin logic. Remove every unsafe item and migrate everything to environment variables only.”

This ensures nothing else is hidden elsewhere.

⸻

3. Regenerate all Firebase Admin keys

Even if you changed your Google password, if ANY Firebase key was leaked in code:

👉 Regenerate the Firebase Admin SDK private key
👉 Delete the old key from Firebase
👉 Update the new key into your Replit environment variables

If you want I will guide you step by step exactly where to click in Firebase.

⸻

4. Re-run WebAuthn + KYC Admin role setup

The new login flow must be fully secure:

✔ Google login
✔ Firebase Auth session
✔ WebAuthn
✔ Admin role check (from Firestore claims)
✔ No backdoor passwords

If anything is wrong, admin login may break, but we can fix it in minutes.

⸻

5. VERY IMPORTANT — rotate your Gmail tokens

Your Google password change automatically invalidates:

✓ Old OAuth refresh tokens
✓ Gmail API tokens
✓ Calendar tokens
✓ Sheets tokens

When the backend tries to use the old tokens, Gmail will reject them.

We will need to:

Generate NEW refresh tokens and update:
GOOGLE_MAIL_REFRESH_TOKEN
GOOGLE_SHEETS_REFRESH_TOKEN
GOOGLE_CALENDAR_REFRESH_TOKEN

If you want, I’ll generate the correct Google OAuth playground flow instructions for you right now.

⸻

6. Tell me if the site is still acting strange

Broken images
Broken layout
Blank pages
Login not working

These are usually from:

✓ Wrong build path (dist/public/…)
✓ Old cached service worker
✓ Missing asset paths
✓ Cloud Run serving wrong folder

I can fix all of that too.
