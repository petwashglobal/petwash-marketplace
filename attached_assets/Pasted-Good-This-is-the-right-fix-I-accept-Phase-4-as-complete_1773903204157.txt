Good. This is the right fix.

I accept Phase 4 as completed.

What is now good:
- V2 action routes exist
- POSJobs writes to V2
- POSDashboard writes to V2
- reads and writes now hit booking_requests
- status transitions are enforced with allowed-from guards
- status_history is appended correctly
- decline / cancel / dispute reasons are now distinguishable
- the dashboard now reflects the same table that provider actions modify

This is the most important integrity fix so far.

Now I want the next phase done with the same proof standard.

1. Proof I still want next
Please send:
- one real authenticated V2 accept action proof
- one real authenticated V2 decline action proof
- one real authenticated V2 report action proof
- one real authenticated V2 start action proof
- one real authenticated V2 complete action proof
- one screenshot of POSJobs after a V2 action
- one screenshot of POSDashboard counts after a V2 action

2. Cleanup required now
Please do these next:
- identify every remaining V1 action route still registered
- mark them as deprecated in code
- add warning logs if they are called
- prepare them for removal in Phase 5

3. Hardening checks
Please confirm:
- cancellation_reason prefixes are consistent everywhere
- status_history entries are always appended, never replaced
- actor and uid are always stored
- report action cannot be called on terminal bookings
- ownership check is enforced in both read and write path
- invalid transitions always return the current status and allowed-from list

4. Next build order
Now do this in order:

A. Real provider flow proofs
- start
- complete
- accept
- decline
- report

B. Phase 5 cleanup
- retire dead V1 action routes
- remove unused V1 action code paths from UI
- reduce legacy booking route surface

C. Booking lifecycle polish
- clearer request states in UI
- clearer active / upcoming / terminal views
- stronger action confirmations and error states

D. Owner-side booking maturity
Once provider booking integrity is stable, move focus to customer-side booking management and repeat loops

5. Important architecture rule
From this point on, booking_requests is the canonical provider booking system.
No new provider booking logic should be added to the old bookings table.

6. Exact deliverables next
Please send:
- exact files changed
- real authenticated action proofs
- list of remaining V1 booking action routes
- deprecation plan for those V1 routes
- any gaps honestly listed