Please do a deep, final pass on the whole PetWash platform with focus on both security and stability in real production.
	1.	Security and secrets: Verify that no API keys, passwords, tokens, or any sensitive data are exposed in the repo, logs, frontend bundle, or error responses. All secrets must come only from environment variables. Remove or lock any debug or test endpoints and make sure every admin route is fully protected by auth, WebAuthn, KYC RBAC, and proper session checks. Confirm CSRF protection is correct for all relevant routes and that session cookies are secure, HTTP only, same site, and consistent across all flows.
	2.	Authentication and sessions: Stress test login, signup, password reset, WebAuthn, Google sign in, and all email flows. Sessions must never randomly drop. Ensure that rate limiting and brute force protection are in place on auth endpoints. Confirm that all user roles (guest, normal user, staff, admin, franchisee) see only what they are allowed to see, nothing more.
	3.	Frontend build and routing: Make sure production always serves the correct built assets from dist/public, including index.html, logos, icons, and all images. There must be no references to old paths like server/public. Fix any 404 images or broken icons on the home page and all sections. Verify SPA routing works for every page and sub page without white screens or blank components.
	4.	Cross device layout and UX: Test the full site on iPhone, iPad, small Android, large Android, and desktop on Safari, Chrome, and Edge. Fix any layout breaks, overlapping text, missing images, or components that do not resize correctly. Navigation, language selector, login, signup, and all main CTAs must be clear and usable on all screen sizes, in both portrait and landscape.
	5.	Flows end to end: For each key journey (public info pages, sign up, login, profile update, bookings, franchise contact, contact form, password reset, logout) run it end to end in production mode and confirm it is smooth, with proper validation, clear error messages, and no dead ends or crashes.
	6.	Logging, errors, and performance: Make sure server logs do not leak sensitive information. Replace any noisy or generic error handling with clear, safe error responses. Confirm that assets are cached correctly, that there are no huge unused scripts, and that the first paint of the homepage is fast and stable.
	7.	Clean up and consistency: Remove unused code, old experiments, or obsolete endpoints that can confuse future work. Keep the structure simple, logical, and ready for 2025 level scale and quality.

When you finish, the result should be a secure, elegant, stable system that behaves the same on all devices, with no broken images or layouts, no security gaps, and no surprises in production.