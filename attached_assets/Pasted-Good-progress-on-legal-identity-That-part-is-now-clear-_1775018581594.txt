Good progress on legal identity. That part is now clear and correct.

Legal identity is FINAL and must not change again:
- Company number (ח.פ.): 517145033
- Hebrew name: פט וואש בע"מ
- English name: PET WASH LTD

You already fixed the 4 files. Good.

Do NOT touch finance/email templates further right now.

---

Now the real problem:

You are still missing the core system logic.

The system is NOT forms and endpoints.

This is an identity system + wallet system + role system.

Right now the mobile signup / login / loyalty entry is broken because there is no unified flow.

---

You must rebuild the signup logic properly.

Follow this EXACT structure:

1. Single unified entry point

User must be able to enter with:
- phone number
- Apple / Face ID
- Google
- email

But internally ALL of these must create ONE unified user.

No separate logic per method.

---

2. Verification is mandatory

- Phone → SMS code verification
- Email → email verification
- Apple/Google → trusted identity

If user is not verified → account must NOT be created.

No silent failures.

---

3. Create user core immediately

System must create:

user {
  id
  phone (optional)
  email (optional)
  auth_provider
  verified: true/false
  created_at
}

---

4. Wallet must be created automatically

Immediately after user creation:

wallet {
  user_id
  balance = 0
  ledger = []
}

User must NEVER be blocked because wallet does not exist.

Wallet is created automatically. Always.

---

5. Assign default role

Every new user must get:

user_roles = ['loyalty_member']

This is mandatory.

Without this, the system has no identity context.

---

6. Consent step (CRITICAL - currently missing)

User MUST explicitly approve:

- Terms of Service
- Privacy Policy
- Marketing consent (optional checkbox)

Right now this is broken:
I did NOT receive any consent screen on mobile.

This is both:
- product failure
- legal issue

You must add it.

---

7. Only after all above → allow actions

Only AFTER:
- user exists
- verified
- wallet created
- role assigned
- consent approved

Then user can:
- wash dog
- book sitter
- use wallet
- earn rewards

---

8. Fix current broken behavior

Right now system likely has these issues:

- Phone auth not completing
- Face ID / Apple not linked to backend user
- No unified user creation
- Consent step missing
- Wallet required before creation (wrong)
- Booking blocked incorrectly
- No clear error messages

Fix all of these.

---

9. Payment and wallet logic (VERY IMPORTANT)

System must behave like this:

- User can ONLY book if wallet >= total cost
- If not → force top-up BEFORE booking
- Works with:
  - Nayax
  - Shift4
  - any provider

Wallet is the SINGLE source of truth.

---

10. Return FULL FLOW PROOF

I do NOT want explanations.

I want exact flow.

Show:

Step-by-step:
- entry → verification → user creation → wallet → role → consent → ready

For each step include:
- API route
- request
- response
- DB changes

Also show:
WHERE the system currently fails.

---

11. Workflow proof (separate track)

After fixing signup, continue:

- Route proof (real responses)
- 6 scenario proof (real DB state before/after)
- Race condition proof (parallel approve → one fails)
- Permission proof:
  - support cannot approve
  - admin can approve
  - user cannot access admin routes

---

Important:

Do NOT mix:
A. signup + identity system
B. provider workflow

Fix signup FIRST.

---

Summary:

You built backend structure well.

But you are missing system thinking.

Now fix:
- identity
- wallet
- roles
- consent
- mobile flow

This is the foundation of the entire platform.