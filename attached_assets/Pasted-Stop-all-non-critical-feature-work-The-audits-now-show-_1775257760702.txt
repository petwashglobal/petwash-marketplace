Stop all non-critical feature work.

The audits now show three production blockers:
1. payment/security integrity
2. marketplace authorization/escrow integrity
3. signup/onboarding architecture fragmentation

I need you to work in this exact order.

PHASE 1 - MONEY + SECURITY P0
Fix first, before anything else:

1. Disable every fake success payment path
- /api/express-checkout must stay disabled
- no endpoint may ever return purchase/payment success without real processor confirmation

2. Remove all fallback secrets and fail open behavior
- Nayax API/webhook/voucher secrets
- unsubscribe HMAC fallback
- any payment/webhook secret fallback
- any IP allowlist that allows traffic when config is missing
- production must fail closed, not fail open

3. Lock down all contractor and invoice privacy surfaces
- contractor onboarding must require auth + ownership
- contractor invoice preview/generate must require auth + ownership/admin
- no plaintext PII storage
- tax ID / national insurance / bank details must be encrypted or rejected

4. Fix webhook trust and replay safety
- persistent dedup in DB/Redis
- longer retention for idempotency
- no signature validation bypass when secret is missing
- no public IP access if allowlist is unset in production

5. Standardize admin auth
- one admin auth method only
- timing-safe compare everywhere
- no ADMIN_SECRET fallback to COOKIE_SECRET
- no mixed x-admin-secret / x-admin-key patterns

PHASE 2 - MARKETPLACE P0/P1
Fix next:

6. Booking ownership and role integrity
- every booking action must verify the user is a real party to the booking or admin
- never trust role from request body/query
- derive actor role from booking record only

7. Prevent illegal completion / payout paths
- no direct user path to completed
- same user cannot complete both sides
- completed must be system-only after both real parties review

8. Fix slot and escrow lifecycle
- payment.failed / expired / cancelled must release slots
- payment.success must move escrow from pending_payment to held
- bookings table must have expiry handling
- no booking or slot may remain stuck forever

9. Resolve escrow fragmentation
- document the authoritative escrow system now
- prevent double release across Firestore / Postgres / BillingEngine flows
- dispute resolution must actually trigger refund or release, not only change status text

10. Fix K9000 truthfulness
- if machine activation is still a stub, customer-facing paid flow must not pretend the wash actually started
- do not show completion/receipt language that implies real machine activation if hardware command is not live

PHASE 3 - SIGNUP / AUTH / PROVIDER ONBOARDING
This is the third emergency track and explains why registration feels broken.

11. Build one shared post-registration bootstrap used by ALL auth methods
Every successful auth path must call one common function that ensures:
- Postgres user exists
- wallet exists
- loyalty exists
- baseline profile exists
- post-login readiness is consistent

This must cover:
- email
- phone
- Google
- Apple
- Facebook
- Instagram
- TikTok
- provider intent

12. Fix phone OTP dead-end
- collect firstName + lastName + email before completion
or
- allow complete-profile safely without blocking loop

13. Create a real /verify-email flow
- page exists
- resend exists
- user is never sent to a dead route

14. Fix social OAuth architecture
- Facebook server callback must exist
- Instagram/TikTok must create Postgres user even without email
- Apple returning users must be found by UID, not only email
- provider intent must survive OAuth redirects
- only one place may consume getRedirectResult()

15. Fix provider onboarding blockers
- guard redirect with !isLoading
- refresh Firebase token after phone verification
- KYC failures must not deadlock in processing
- background/police/KYC steps must not be treated as complete when they are stubs
- Walk My Pet / Academy provider self-registration must not create active providers without real verification

PHASE 4 - STRUCTURAL CLEANUP
After the above, then fix:

16. Role model fragmentation
- choose one authoritative role source
- users.role / users.roles / users.permissions / userRoles must not conflict

17. Booking/status fragmentation
- reduce parallel state systems
- normalize customer-facing booking status across platforms

18. Invoice/document correctness
- sequential numbering
- clear commission vs customer-paid amount labeling
- no misleading tax/receipt wording
- dispute/admin tooling for financial resolution

19. Tests for critical flows
Add tests for:
- payment success/failure
- webhook replay/idempotency
- booking ownership
- escrow transitions
- refunds
- signup bootstrap
- social auth completion
- provider onboarding gates

DELIVERY FORMAT
For each fix, I want:
- exact file changed
- exact commit
- before behavior
- after behavior
- proof of fix
- whether migration, backfill, secret rotation, or data repair is required

SPECIAL REPORTS REQUIRED
After Phase 1:
- which secrets were removed/rotated
- which endpoints were disabled
- whether any plaintext sensitive data existed historically
- whether any fake payment path was ever reachable from UI

After Phase 2:
- which booking/escrow paths are now safe
- what remains split across systems

After Phase 3:
- which auth/signup paths are now unified
- whether existing users need wallet/loyalty/profile backfill

Bottom line:
The repo is currently unsafe not because of design, but because of money integrity, permissions, escrow lifecycle, and fragmented registration architecture.

Fix these before any further product work.