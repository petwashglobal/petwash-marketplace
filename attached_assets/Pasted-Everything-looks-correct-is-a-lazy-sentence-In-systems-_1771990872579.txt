Everything looks correct” is a lazy sentence. In systems like this, “looks correct” still means “we can still get surprise billing, silent failures, and production incidents.”

What is acceptable

He finally provided:
	•	Real file paths
	•	Real code
	•	Real rate limiter config
	•	Session token forwarding with validation
	•	Origin enforcement

That’s the minimum standard.

What is still NOT acceptable and must be tightened

1) Origin allowlist is currently sloppy

If he used source.includes("petwash.co.il") it is insecure.

Because:
	•	petwash.co.il.evil.com will pass.
	•	So will evil.com/?next=petwash.co.il

This is a real security hole. Not theoretical.

Fix required:
	•	Parse hostname and match exact domain or subdomain only.

If he won’t fix this, the origin enforcement is basically “security theater”.

⸻

2) The “x-internal-service” header is not security

Any attacker can send that header. Headers are not trusted.

If he allows requests with no Origin/Referer only based on that header, that is weak.

Fix required:
One of these:
	•	Require a secret key header stored server-side (not in frontend)
	•	Or require Cloud Run service-to-service identity
	•	Or only allow no-origin calls from private internal network ranges

Right now it’s not strong enough.

⸻

3) No real proof from DevTools means it’s not “done”

If he can’t show a real Network request from the live UI:
	•	then he does not actually know it works end to end.
	•	he only knows the code compiles.

Proof required:
	•	A screenshot of DevTools Network showing /api/google/places-autocomplete
	•	Status 200
	•	Predictions array returned
	•	No CORS errors

Until then, nobody should call this “fixed”.

⸻

4) Rate limit values might still be too generous

100 requests per minute per IP is fine for normal use,
but if a single user types fast or an attacker rotates IPs, you still burn money.

Required:
	•	Add per-session or per-user limits too, not only per IP.
	•	And log usage spikes.

⸻

What you should reply to him (firm, no emotion)

Tell him this, exactly:
	1.	Replace includes() origin checks with strict hostname matching (exact domain or subdomain only).
	2.	Remove reliance on x-internal-service header alone, it is spoofable. Use a server secret or Cloud Run identity.
	3.	Provide DevTools proof from the live UI showing real requests and responses. No proof, not accepted.
	4.	Add a second limiter keyed by session or user in addition to IP.
	5.	Add an alert or logging dashboard for abnormal Places usage spikes.

If he does these, then we can say “done”. If not, it’s not done.

If you paste his origin check code snippet exactly, I’ll rewrite it correctly in 5 lines.