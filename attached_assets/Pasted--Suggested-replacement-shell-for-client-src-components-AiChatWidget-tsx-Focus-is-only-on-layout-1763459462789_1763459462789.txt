// Suggested replacement shell for client/src/components/AiChatWidget.tsx
// Focus is only on layout and safe positioning. 
// Please keep all existing chat internals, but wrap them with this container.

import React, { useState } from "react";

// Approximate height of the 3 floating round buttons area (WhatsApp, AI, Accessibility)
const FAB_STACK_HEIGHT_PX = 96; // adjust if the buttons are taller

export default function AiChatWidget() {
  const [open, setOpen] = useState(false);

  // Closed state – small launcher button
  if (!open) {
    return (
      <button
        type="button"
        aria-label="Open Pet Wash AI chat"
        className="fixed right-4 bottom-4 md:right-6 md:bottom-6 z-40 h-14 w-14 rounded-full bg-slate-900 text-white text-sm font-semibold shadow-lg"
        style={{
          paddingBottom: "env(safe-area-inset-bottom)",
        }}
        onClick={() => setOpen(true)}
      >
        AI
      </button>
    );
  }

  // Open state – main chat window
  return (
    <div
      className="fixed z-50 flex flex-col rounded-2xl bg-white shadow-2xl border border-slate-200"
      style={{
        // Horizontal position
        right: "1rem",
        // Sit above iOS safe area + floating button stack
        bottom: `calc(env(safe-area-inset-bottom) + ${FAB_STACK_HEIGHT_PX}px)`,
        // Width and height responsive rules
        width: "min(420px, 100vw - 2rem)",
        maxHeight: "min(80vh, 600px)",
      }}
    >
      {/* Header bar */}
      <header className="flex items-center justify-between px-4 py-3 border-b border-slate-100">
        <div className="text-sm font-semibold">Pet Wash AI assistant</div>
        <button
          type="button"
          onClick={() => setOpen(false)}
          className="text-xs text-slate-500 hover:text-slate-800"
        >
          Close
        </button>
      </header>

      {/* Chat body – keep existing chat component here */}
      <div className="flex-1 overflow-y-auto px-4 py-3 text-sm">
        {/* TODO: mount existing chat thread component instead of this placeholder */}
        <p className="text-slate-500">
          Chat content goes here. Please mount the current Pet Wash AI chat
          component inside this container.
        </p>
      </div>

      {/* Input area – keep your existing input component here */}
      <footer className="border-t border-slate-100 px-3 py-2">
        {/* TODO: mount existing chat input component */}
      </footer>
    </div>
  );
}