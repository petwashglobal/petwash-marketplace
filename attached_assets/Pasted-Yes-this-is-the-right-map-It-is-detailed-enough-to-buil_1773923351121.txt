Yes - this is the right map.

It is detailed enough to build from without guessing, and the rollout order is correct.

The best next move is to start exactly with:

1. DB + seed
	•	create the new tables
	•	seed loyalty_rules
	•	add the user balance and referral fields
	•	keep all rewards disabled by config until the engine is ready

Then:

2. loyalty ledger engine
	•	one canonical service for credit, debit, expiry, cap checks
	•	no reward logic spread across routes

Then:

3. booking-complete trigger wiring
	•	second booking reward
	•	streak rewards
	•	referral completion credit
	•	win-back reset on conversion

That gives you a safe base before UI and experiments.

A few important implementation decisions I would lock before coding:
	•	Keep all money in cents only
	•	Make ledger append-only and derive trust from events, never silent overwrites
	•	Treat users.loyalty_balance_cents as a cache, not source of truth
	•	Put all rule amounts and expiry in loyalty_rules, not hardcoded
	•	Keep one discount source active at checkout unless you explicitly choose stacking later
	•	Add a strict idempotency key on reward events tied to user_id + rule_key + booking_id so rewards cannot double-fire

The only refinement I would add to your map is one extra table or constraint for safety:

reward_claims
	•	id
	•	user_id
	•	rule_key
	•	booking_id nullable
	•	referral_id nullable
	•	unique_fingerprint unique
	•	created_at

That prevents duplicate credits cleanly, especially for retries, cron reruns, and race conditions.

So the exact coding order I recommend is:

Phase 6.1
	•	DB schema
	•	seed rules
	•	reward idempotency layer

Phase 6.2
	•	loyalty ledger service
	•	expiry service
	•	balance read endpoints

Phase 6.3
	•	booking completion triggers
	•	referral triggers

Phase 6.4
	•	win-back queue + scheduler
	•	notification inserts

Phase 6.5
	•	quote preview redemption
	•	checkout apply/remove credits

Phase 6.6
	•	customer UI
	•	wallet, streaks, referral card

Phase 6.7
	•	admin controls
	•	experiment dashboard

So yes - start coding at item 1 now.