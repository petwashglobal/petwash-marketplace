We are now completing the Provider Search system for the marketplace service domains only.

IMPORTANT ARCHITECTURE RULE:
This work applies ONLY to marketplace services (pet sitting, dog walking, grooming, transport, daycare).
DO NOT reuse any of this logic for K9000. K9000 is a separate machine domain.

--------------------------------------
1. DATABASE VALIDATION (CRITICAL)
--------------------------------------

Verify the following fields exist and are correctly populated:

providers:
- id
- userId
- platformId
- verificationStatus
- city / suburb / postcode
- lat / lng (must exist for distance ranking)

walker_profiles:
- providerId
- baseHourlyRate
- averageRating
- completedBookings
- responseTimeMinutes

sitter_profiles:
- providerId
- service_types (must include mapping to pet_sitting and daycare)
- baseRate
- rating
- completedBookings

provider_operational_settings:
- userId
- instantBooking (boolean)

If lat/lng is missing → fallback is allowed but must log warning.

--------------------------------------
2. SERVICE DOMAIN MAPPING (DO NOT BREAK)
--------------------------------------

Ensure mapping is strict:

walk_my_pet → dog_walking
sitter_suite → pet_sitting / daycare (based on service_types)
groomers → grooming
pet_trek → transport

Do NOT merge domains incorrectly.

--------------------------------------
3. GEOCODING (ALREADY WIRED – VERIFY)
--------------------------------------

resolveSearchLocation must:

1. Use lat/lng from frontend if provided
2. Otherwise:
   - Call Google Maps Geocoding API
   - language=he
   - region=IL
   - components=country:IL
3. Return lat/lng for scoring

Add logging:
- log locationSource (input / query_geocode / none)

--------------------------------------
4. SEARCH LOGIC VALIDATION
--------------------------------------

Ensure the following works correctly:

- Open browse (NO dates)
- Date filtering (WITH dates)
- Radius filter (hard cutoff)
- Distance scoring (Haversine)
- Rating filter
- Price filter
- Verified filter
- Instant booking filter
- Sorting modes:
  - recommended
  - closest
  - top_rated
  - lowest_price
  - fastest_response

--------------------------------------
5. FRONTEND WIRING (CRITICAL UX)
--------------------------------------

Ensure UI works on mobile + desktop:

- Filter panel:
  - radius slider
  - rating dropdown
  - price range
  - verified toggle
  - instant booking toggle
  - sort dropdown

- Mobile:
  - bottom sheet filter
- Desktop:
  - left sidebar filter

- URL must sync with filters (deep linking)

--------------------------------------
6. EMPTY STATE + DEBUGGING
--------------------------------------

If 0 results:
- Show user-friendly message
- Log debug info:
  - filters used
  - location source
  - number of providers fetched before filtering

--------------------------------------
7. LOGGING (IMPORTANT FOR DEBUG)
--------------------------------------

Add logs:

[ProviderSearch]
- filters received
- providers fetched per domain
- after filtering count
- after ranking count
- location source

--------------------------------------
8. PERFORMANCE (DO NOT IGNORE)
--------------------------------------

- Use Promise.all for multi-domain queries
- Do NOT block response on geocoding failure
- Limit pageSize (max 50)

--------------------------------------
9. STRICT DOMAIN SAFETY RULE
--------------------------------------

NEVER:
- use bookings table for K9000
- use machine tables in this search
- mix kiosk logic into marketplace search

--------------------------------------
10. FINAL TEST CHECKLIST
--------------------------------------

Test all:

- search by city
- search by postcode
- search with no dates
- search with dates
- each service type individually
- mixed service search
- mobile UI
- desktop UI

--------------------------------------
DONE = ONLY WHEN:
--------------------------------------

- real DB data appears in results
- filters visibly change results
- distance sorting works
- no crashes with missing location
- clean separation from K9000 remains