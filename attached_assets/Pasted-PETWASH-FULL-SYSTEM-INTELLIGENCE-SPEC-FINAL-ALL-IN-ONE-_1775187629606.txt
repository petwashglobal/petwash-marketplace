PETWASH FULL SYSTEM INTELLIGENCE SPEC (FINAL - ALL IN ONE)

CORE PRINCIPLE:
The system must evolve from forms and gates into an intelligent, predictive, guided platform.

FINAL RULES:
	•	Do not block users early
	•	Do not rely on postcode
	•	Always use lat/lng
	•	Always reduce user thinking
	•	Always guide instead of forcing

⸻

ADDRESS SYSTEM (FOUNDATION)

Address object:
{
label: “Home”,
address: “1 Collins St, Melbourne”,
lat: -37.8136,
lng: 144.9631,
postcode: “3000” // optional
}

Input flow:
	1.	Use current location (GPS)
	2.	Saved addresses (history)
	3.	Google autocomplete

Critical rule:
if (!postcode) {
// never block
proceedWithLatLng()
}

Distance logic:
distance = calculateDistance(user.lat, user.lng, provider.lat, provider.lng)

⸻

CUSTOMER EXPERIENCE (SMART MATCHING)

Replace manual search with system matching.

Flow:
input = {
serviceType,
date,
time,
location
}

results = matchProviders(input)

Matching algorithm:
score =
distanceScore +
availabilityScore +
ratingScore +
repeatCustomerScore +
responseSpeedScore

UI:
	•	Best Match ⭐
	•	Great Option
	•	Budget Option

Result:
User feels guided, not forced.

⸻

PROVIDER SYSTEM (OPERATING PANEL)

Provider dashboard:
dashboard = {
todayEarnings,
upcomingBookings,
completedJobs,
alerts,
optimizationSuggestions
}

Examples:
	•	“You missed 2 bookings”
	•	“Increase availability → +23% bookings”

⸻

ADMIN SYSTEM (OCTOPUS CONTROL)

Admin panel:
adminPanel = {
activeUsers,
liveBookings,
dropOffPoints,
systemHealth
}

Controls:
	•	toggle friction layers
	•	adjust matching radius
	•	boost providers
	•	pause providers

⸻

USER INTELLIGENCE (CORE BRAIN)

userProfile = {
type,
trustScore,
behaviorScore,
bookingHistory,
preferences,
riskLevel
}

Examples:
	•	auto-fill preferences
	•	show best options first

⸻

TRUST SYSTEM

Customer:
	•	reliability score
	•	cancellation rate

Provider:
	•	rating
	•	response time
	•	verified badge

Example:
customer.trust = 4.5
provider.responseTime = “5 min”

⸻

ONBOARDING (FIXED)

if (socialLogin) {
autoAssignRole(“customer”)
seedNameFromFirebase()
skipOnboarding()
}

Rules:
	•	browsing allowed without full onboarding
	•	booking requires minimal data only

⸻

AUTH SIMPLIFICATION

Keep:
	•	Google
	•	Apple
	•	Email

Remove:
	•	double captcha
	•	forced SMS upfront

SMS only for:
	•	payouts
	•	risky actions
	•	provider verification

⸻

STATE MACHINE

Customer:
visitor → browsing → authenticated → ready_to_book → booked

Provider:
applied → under_review → approved → active → paused

⸻

REAL-TIME COMMUNICATION

emit(“matching.started”)
emit(“provider.accepted”)
emit(“provider.arriving”)

UI:
	•	“Finding your match…”
	•	“Provider accepted your booking”

⸻

RECOVERY SYSTEM (IMPORTANT)

if (signup_not_completed) sendReminder()
if (booking_abandoned) sendFollowUp()
if (inactive_30_days) sendReengagement()

⸻

CRM + EMAIL

HubSpot:
syncToHubSpot(user)

SendGrid:
sendEmail(eventType, user)

Events:
	•	welcome
	•	incomplete signup
	•	booking started
	•	booking completed

⸻

PERFORMANCE RULES
	•	no blocking loaders
	•	instant UI response
	•	optimistic updates
	•	caching

⸻

NAVIGATION

Google:
https://www.google.com/maps?q=LAT,LNG

Waze:
https://waze.com/ul?ll=LAT,LNG&navigate=yes

⸻

FINAL RULE

This system must NOT feel like:
	•	a form
	•	a compliance process

It MUST feel like:
	•	an assistant
	•	a smart marketplace
	•	a guided experience

⸻

FINAL MESSAGE TO DEV

We are no longer fixing bugs.

We are building an intelligent system.

Next phase:
	1.	smart matching engine
	2.	provider dashboard
	3.	admin control system
	4.	unified user intelligence
	5.	trust system
	6.	real-time communication
	7.	recovery automation
	8.	state machine logic
	9.	high performance UX

END